package sample.example.mq;

/**
 * 
 * @author gourabroy
 *
 * This would listen to a port. Publishers would push messages to it and it would notify the Consumers of those messages. 
 * There would be topics on which messages would be published and subscribed to. Messages would be transported over NIO socket channels.
 *  
 *
 *
 */
public class Server {
	
}
